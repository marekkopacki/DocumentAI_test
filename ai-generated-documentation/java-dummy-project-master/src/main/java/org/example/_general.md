![Capgemini Logo](https://www.capgemini.com/wp-content/themes/capgemini2020/assets/images/logo.svg)

### Make it real.

---
 ## General Documentation for example package
Description should have at least 500 characters. Documentation generated based on the provided Markdown files. Aggregate all descriptions from those files.

The `example` package contains two Java classes, `Client` and `Service`, that demonstrate basic and advanced functionalities. The `Client` class takes a `Service` object as a constructor argument and provides a method called `greeting(String name)`. The `Service` class contains methods for checking if an integer is even (`isEven(int input)`) and printing out the signs of three integers (`highComplexityMethod(int a, int b, int c)`).

## Table of Contents
- [Client.md](Client.md)
  - **Description:** This file contains detailed documentation for the `Client` class, including its constructor and `greeting` method. It explains how to initialize a new instance of the `Client` class with a `Service` object and generate a greeting message based on the length of the input name.
- [Service.md](Service.md)
  - **Description:** This document provides detailed documentation for the `Service` class, including its `isEven(int input)` and `highComplexityMethod(int a, int b, int c)` methods. It explains how to check if an integer is even and print out the signs of three integers using the provided methods.