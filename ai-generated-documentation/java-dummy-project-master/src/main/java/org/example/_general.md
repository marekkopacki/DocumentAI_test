![Capgemini Logo](https://www.capgemini.com/wp-content/themes/capgemini2020/assets/images/logo.svg)

### Make it real.

---

## General Documentation for example package
Description generated based on the provided Markdown files. The `example` package consists of two main classes, `Client` and `Service`, which work together to generate personalized greetings based on the length of the input name. The `Client` class interacts with the `Service` object to perform this task.

## Table of Contents
- [Client.md](Client.md)
  - **Description:** The `Client` class interacts with a `Service` object to generate personalized greetings based on the length of the input name. If the name's length is even, the greeting is returned in uppercase; otherwise, it is returned in the original case.
- [Service.md](Service.md)
  - **Description:** The `Service` class provides two methods: `isEven` to check if an integer is even and `highComplexityMethod` to print statements based on the signs of three integers (positive, non-positive, or zero).